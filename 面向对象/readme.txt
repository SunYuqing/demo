01.把面向过程的程序改写成面向对象的的形式：
    原则：不能有嵌套函数，但可以有全局变量。
    过程： onload  →构造函数
           全局变量 →属性
           函数    →方法
02.this 容易出错的地方：
        定时器：被定时器调用的函数，this代表window;j
                解决方法：再套一层，使用that代替this
        面向对象